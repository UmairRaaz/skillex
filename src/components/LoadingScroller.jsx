import React from "react";

const LoadingScroller = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <div class="loader"></div>
    </div>
  );
};

export default LoadingScroller;
